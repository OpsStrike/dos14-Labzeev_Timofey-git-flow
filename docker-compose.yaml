version: "3.4"

services:
  pythonapp:
    build:
      context: .
      dockerfile: Dockerfile
  
    volumes:
      - CDdata:/home/bank/git

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    ports:
      - "80:80"
    depends_on:
      - pythonapp

volumes:
  CDdata:



