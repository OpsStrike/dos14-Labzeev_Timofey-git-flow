FROM postgres:15-bullseye

RUN useradd -d /home/bank -U -m -u 1111 postgres && mkdir /home/postgres
    
USER postgres 
    
COPY ./secrets_decrypted.yml /home/postgres

ENV PGDATA=/var/lib/postgresql/data/pgdata

ENV POSTGRES_PASSWORD_FILE=/secrets_decrypted.yml

CMD ["postgres"]

